# Seed corpus

Place binary frames derived from `vectors/codec-v1.json` here. CI and local fuzz
runs may add minimized canonical, boundary and regression cases; files in this
directory are intentionally tracked while generated artifacts are ignored.
